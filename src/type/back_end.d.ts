interface IUsers {
  auth: boolean;
}

interface IProducts {
  id: number,
  img: string,
  name: string,
  brand: string,
  price: number,
  quantity: number,
  category: string,
  description: string
}